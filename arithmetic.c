#include<stdio.h>
int main()
{
    int a=4,b=30;
    // int sum = a+b;
    // int mines = b-a;
    // int multi = a*b;
    // float divided = b*1.0/a;
    int mod = b%a;

    printf("%d",mod);

    return 0;
}